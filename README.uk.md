# SecretChat

[English](README.md) | [Українська](README.uk.md)

Невеликий pet-проєкт захищеного чату в браузері. Користувачі авторизуються через HTTP API, створюють спільні чати та обмінюються повідомленнями в реальному часі через WebSocket.

SecretChat задуманий як навчальний проєкт для приватного спілкування: сервер керує користувачами, чатами та підключеннями, а формат повідомлень залишається простим і зручним для клієнтських застосунків.

## Можливості

- реєстрація та авторизація користувачів;
- JWT-авторизація через cookies;
- створення та редагування чатів;
- додавання учасників до чату;
- обмін повідомленнями в реальному часі через WebSocket;
- асинхронна робота з PostgreSQL;
- окремий React-клієнт на базі Vite.

## Технології

**Backend:** Python 3.13+, FastAPI, WebSockets, SQLAlchemy, Alembic, PostgreSQL, Pydantic, `cryptography`.

**Frontend:** React, Vite, JavaScript.

## Запуск проєкту

### 1. Налаштувати середовище

Створіть файл `.env` у корені проєкту:

```env
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=secretchat
DEBUG=true
JWT_SECRET_KEY=change-me-to-a-long-random-secret
```

Встановіть Python-залежності та активуйте віртуальне середовище. Наприклад, за допомогою `uv`:

```bash
uv sync
```

### 2. Запустити backend

```bash
uv run uvicorn main:app --reload
```

API буде доступний за адресою `http://127.0.0.1:8000`.

### 3. Запустити frontend

```bash
cd frontend
npm install
npm run dev
```

Клієнт Vite буде доступний за адресою `http://localhost:5173`.

## Основні HTTP-маршрути

| Метод | Маршрут | Призначення |
| --- | --- | --- |
| `POST` | `/user` | Створити користувача |
| `POST` | `/auth` | Увійти та отримати auth cookies |
| `GET` | `/user/{id}` | Отримати користувача |
| `POST` | `/chat` | Створити чат |
| `GET` | `/chat` | Отримати чати поточного користувача |
| `GET` | `/chat/{chat_id}` | Отримати чат за ідентифікатором |
| `PATCH` | `/chat` | Оновити чат |

## WebSocket-протокол

Після авторизації клієнт підключається до:

```text
ws://127.0.0.1:8000/ws
```

Авторизаційні cookies мають передаватися разом із WebSocket-підключенням.

### Повідомлення від клієнта

Кожне повідомлення містить тип події та дані повідомлення. Клієнт генерує `client_msg_id`, щоб зіставити надіслане повідомлення з відповіддю сервера.

```json
{
  "type": "message",
  "data": {
    "chat_id": 123,
    "client_msg_id": "5b1e...uuid",
    "body": "Привіт!"
  }
}
```

### Повідомлення від сервера

Сервер розсилає подію учасникам відповідного чату:

```json
{
  "type": "message",
  "data": {
    "id": 456,
    "chat_id": 123,
    "client_msg_id": "5b1e...uuid",
    "body": "Привіт!"
  }
}
```

## Структура проєкту

```text
.
├── chat/       # WebSocket-підключення, чати та повідомлення
├── users/      # Користувачі, реєстрація та авторизація
├── core/       # Налаштування, залежності та база даних
├── frontend/   # React-клієнт
└── main.py     # Точка входу FastAPI
```

## Статус шифрування

SecretChat розвивається як проєкт захищеного спілкування. Доступ до WebSocket захищений авторизацією, а залежність `cryptography` вже передбачена для подальшої реалізації шифрування. У production-середовищі з'єднання потрібно запускати через `wss://` за HTTPS-проксі та окремо реалізувати end-to-end шифрування повідомлень, якщо сервер не повинен мати доступу до їхнього вмісту.

## Ідеї для розвитку

- повноцінне end-to-end шифрування повідомлень;
- історія повідомлень і пагінація;
- статуси доставки та прочитання;
- індикатор "друкує...";
- приватні повідомлення та групові чати;